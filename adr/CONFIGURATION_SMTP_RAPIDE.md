# Archive - SMTP rapide (obsolete)

Le flux SMTP direct n'est plus utilise.

## Remplacement

Utiliser `RESEND_SETUP.md` :

- mobile -> `POST /api/public/signalement-email`
- BackOffice -> Resend cote serveur
